import theme from "../../common/theme";

export default {

  app: {
    minHeight: '100%'
  },
  name: {
    fontSize: 'calc(16px + 2vmin)',
    fontWeight: 600,
    color: theme.palette.secondary.main,
    paddingLeft: '10px'
  }
  
};